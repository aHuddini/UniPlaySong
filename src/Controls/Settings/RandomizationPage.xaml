<UserControl x:Class="UniPlaySong.Controls.Settings.RandomizationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UniPlaySong"
             xmlns:ups="clr-namespace:UniPlaySong.Controls.Settings"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/UniPlaySong;component/Controls/Settings/SettingsResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Everything that decides which song comes next, and how long the current one gets.
         Preview Mode belongs here rather than on its own page: it is the third answer to
         "when does this track stop", alongside randomize-on-end and stop-on-end. -->

    <ScrollViewer Style="{StaticResource UpsPage}">
        <StackPanel MaxWidth="820" HorizontalAlignment="Stretch">

            <TextBlock Text="Randomization" Style="{StaticResource UpsPageTitle}"/>
            <TextBlock Style="{StaticResource UpsPageSubtitle}"
                       Text="Which song plays next when a game has several, and how long each one gets."/>

            <Border Style="{StaticResource UpsSectionBar}">
                <TextBlock ups:TrackedCaps.Text="Song Selection" Style="{StaticResource UpsSectionTitle}"/>
            </Border>

            <CheckBox Content="Pick a new song each time you select the game"
                      Style="{StaticResource UpsToggle}"
                      IsChecked="{Binding Settings.RandomizeOnEverySelect}"/>
            <TextBlock Style="{StaticResource UpsHint}"
                       Text="After the game's primary song has played once, later visits pick at random from the rest."/>

            <CheckBox Content="Pick a new song when the current one ends"
                      Style="{StaticResource UpsToggle}"
                      IsChecked="{Binding Settings.RandomizeOnMusicEnd}"/>
            <TextBlock Style="{StaticResource UpsHint}"
                       Text="When a track finishes, another is chosen at random from the game's songs instead of looping."/>

            <CheckBox Content="Stop when the song ends"
                      Style="{StaticResource UpsToggle}"
                      IsChecked="{Binding Settings.StopAfterSongEnds}"/>
            <TextBlock Style="{StaticResource UpsHint}"
                       Text="No loop and no advance — silence until you select another game. Applies to normal playback and to preview mode."/>

            <Border Style="{StaticResource UpsSectionBar}">
                <TextBlock ups:TrackedCaps.Text="Preview Mode" Style="{StaticResource UpsSectionTitle}"/>
            </Border>

            <CheckBox Content="Limit how long game music plays"
                      Style="{StaticResource UpsToggle}"
                      IsChecked="{Binding Settings.EnablePreviewMode}"/>
            <TextBlock Style="{StaticResource UpsHint}"
                       Text="Game music restarts once it hits the length below, so you hear the opening of a track rather than all of it. Default music is unaffected."/>
            <StackPanel Orientation="Horizontal" Margin="32,0,0,14"
                        IsEnabled="{Binding Settings.EnablePreviewMode}">
                <TextBlock Text="Preview length" Style="{StaticResource UpsLabel}" Margin="0,0,12,0"/>
                <Slider Minimum="15" Maximum="300" Width="220" VerticalAlignment="Center"
                        Value="{Binding Settings.PreviewDuration}"
                        SmallChange="5" LargeChange="15" TickFrequency="15"/>
                <TextBlock Style="{StaticResource UpsValue}" Margin="12,0,0,0"
                           Text="{Binding Settings.PreviewDuration, StringFormat='{}{0} s'}"/>
            </StackPanel>

            <Border Style="{StaticResource UpsSectionBar}">
                <TextBlock ups:TrackedCaps.Text="Random Game Picker" Style="{StaticResource UpsSectionTitle}"/>
            </Border>

            <CheckBox Content="Play music while browsing the random game picker"
                      Style="{StaticResource UpsToggle}"
                      IsChecked="{Binding Settings.EnableRandomPickerMusic}"/>
            <TextBlock Style="{StaticResource UpsHint}"
                       Text="Each game's music plays as you click Pick Another in Playnite's picker dialog. Music returns to your library selection when the dialog closes."/>

        </StackPanel>
    </ScrollViewer>
</UserControl>
