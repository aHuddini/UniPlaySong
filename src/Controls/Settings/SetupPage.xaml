<UserControl x:Class="UniPlaySong.Controls.Settings.SetupPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UniPlaySong"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/UniPlaySong;component/Controls/Settings/SettingsResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <!-- Setup Tab: one place for the external tools UPS shells out to. Deliberately has no
         "Reset to Defaults" button — these are machine-specific paths the user had to locate,
         not preferences, and the global reset preserves them for the same reason. -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20">
            <TextBlock Text="Setup" FontSize="18" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="Point UniPlaySong at the external tools it needs. Set these once and every other feature that depends on them will work."
                       FontSize="11" Foreground="#90A8A0" FontStyle="Italic" TextWrapping="Wrap" Margin="0,0,0,20"/>

            <TextBlock Text="yt-dlp" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="yt-dlp Path: " VerticalAlignment="Center" Width="120"/>
                <TextBox Text="{Binding Settings.YtDlpPath}"
                         Width="300"
                         Margin="0,0,10,0"
                         IsReadOnly="False"/>
                <Button Content="Browse..."
                        Width="80"
                        Command="{Binding BrowseForYtDlpFile}"/>
                <TextBlock Text="{Binding YtDlpStatus}" Foreground="{Binding YtDlpStatusBrush}" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold"/>
            </StackPanel>
            <TextBlock Text="Point this to the full path of yt-dlp.exe (e.g. C:\Tools\yt-dlp\yt-dlp.exe). For YouTube downloads, place deno.exe in the same folder as yt-dlp.exe. Yt-dlp requires a JavaScript runtime (Deno recommended) for YouTube support. Used for downloading music."
                       FontSize="11"
                       Foreground="Gray"
                       TextWrapping="Wrap"
                       Margin="20,0,0,20"/>

            <Separator Margin="0,10"/>

            <TextBlock Text="FFmpeg" FontSize="14" FontWeight="SemiBold" Margin="0,10,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="FFmpeg Path: " VerticalAlignment="Center" Width="120"/>
                <TextBox Text="{Binding Settings.FFmpegPath}"
                         Width="300"
                         Margin="0,0,10,0"
                         IsReadOnly="False"/>
                <Button Content="Browse..."
                        Width="80"
                        Command="{Binding BrowseForFFmpegFile}"/>
                <TextBlock Text="{Binding FfmpegStatus}" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="SemiBold"/>
            </StackPanel>
            <TextBlock Text="Point this to the full path of ffmpeg.exe (e.g. C:\Tools\ffmpeg\ffmpeg.exe). Required for music downloads (audio extraction/conversion) as well as normalization, silence trimming, precise trim, audio repair, and trailer audio extraction. Download from ffmpeg.org if not installed."
                       FontSize="11"
                       Foreground="Gray"
                       TextWrapping="Wrap"
                       Margin="20,0,0,20"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
